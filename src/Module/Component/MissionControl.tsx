import SidebarTabs from "./SidebarTabs";


// todo: added as draft - we may use for later - decided to ignore for now. 
function MissionControl(props: any) {
    return (
        <div>
            <SidebarTabs mapWindow={props.mapWindow}>
                
            </SidebarTabs>
        </div>
    );
}

export default MissionControl;